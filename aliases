alias ls='ls -G'
alias la='ls -A'
alias ll='ls -l'
alias lla='ls -lA'

alias update_host='python ~/git/hosts/updateHostsFile.py -e gambling porn -f -r'

# Enable aliases to be sudo’ed
alias sudo='sudo '

# Get OS X Software Updates, and update installed Homebrew, its installed packages
alias update='printf "check apple update\n"; sudo softwareupdate -i -a; printf "update brew\n"; brew update -v; brew upgrade; brew_cask_upgrade; brew cleanup; brew cask cleanup; brew doctor; brew prune; printf "update npm\n"; npm install npm -g; npm update -g;'

